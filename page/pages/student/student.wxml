<!--pages/student/student.wxml-->
<view class='container'>

  <view class='tab-wrapper'>
    <view id='tableft' class='tab-left {{currentTab === 0 ? "tab-active":""}}' bindtap='switchTab'>所有课程</view>
    <view id='tabright' class='tab-right {{currentTab === 1 ? "tab-active" : ""}}' bindtap='switchTab'>我的课表</view>
  </view>

  <view class='content-wrapper' wx:if='{{currentTab === 0}}'><allCourse id='allCourse'/></view>
  <view class='content-wrapper' wx:if='{{currentTab === 1}}'><myCourse/></view>

</view>
